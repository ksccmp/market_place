<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- 웹과 관련된 컴포넌트 스캔 -->
	
         <context:component-scan
            base-package="com.ssafy.controller"/>

	
	<!-- 애노테이션 기반으로 동작하라!! 기본 빈 로딩(HandlerMapping, HandlerAdapter) -->
    <mvc:annotation-driven />
    
    <!-- location: 실제 물리 경로, mapping: 연결할 가상 경로 -->
    <mvc:resources location="/WEB-INF/css/" mapping="/css/**"></mvc:resources>
    <mvc:resources location="/WEB-INF/img/" mapping="/img/**"></mvc:resources> <!-- mapping에서 실행되는 path는 location에서 확인해라! -->

	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
	        <!-- Example: a logical view name of 'showMessage' is mapped to '/WEB-INF/view/showMessage.jsp' -->
	        <property name="prefix" value="/WEB-INF/view/"/>
	        <property name="suffix" value=".jsp"/>
	</bean>
	
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/*"/>
			<bean class="com.ssafy.controller.PerformanceInterceptor"></bean>
		</mvc:interceptor>
	</mvc:interceptors>

</beans>
